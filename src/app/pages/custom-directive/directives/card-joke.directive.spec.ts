import { CardJokeDirective } from './card-joke.directive';

describe('CardJokeDirective', () => {
  it('should create an instance', () => {
    const directive = new CardJokeDirective();
    expect(directive).toBeTruthy();
  });
});
